// @flow
const BEHAVIOR = Object.freeze({
  fluid: 'fluid',
  fixed: 'fixed',
});

const ALIGNMENT = Object.freeze({
  start: 'flex-start',
  center: 'center',
  end: 'flex-end',
});

export const LAYOUT_GRID_CONSTANTS = {
  BEHAVIOR,
  ALIGNMENT,
};
