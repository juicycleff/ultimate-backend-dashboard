import * as React from 'react';
import Styled{{ properCase name }} from './styled/{{ kebabCase name }}.styled';

interface Props {
path?: string;
}

const {{ properCase name }}: React.FC<Props> = ({ children }) => (
  <Styled{{ properCase name }}>
    {children}
  </Styled{{ properCase name }}>
);

export default {{ properCase name }};
