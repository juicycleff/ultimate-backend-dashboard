// @flow
import * as React from 'react';
import {styled} from 'src/styles';

const {{ properCase name }}Cmp = styled('div', () => {
  return {};
});

type {{ properCase name }}Prop = {
  children?: any,
};

export const {{ properCase name }} = (props: {{ properCase name }}Prop) => <{{ properCase name }}Cmp>{{ properCase name }}</{{ properCase name }}Cmp>;
